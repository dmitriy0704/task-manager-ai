spring:
  application:
    name: task-manager-ai
  datasource:
    url: jdbc:postgresql://localhost:5432/tasks?serverTimezone=Europe/Moscow
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: dv9899
  jpa:
    hibernate:
      ddl-auto: update
      jdbc:
        time_zone: Europe/Moscow
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        options:
          model: qwen2.5-coder:7b
          format: json
server:
  port: 8090